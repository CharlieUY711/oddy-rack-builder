// Sprint 3 — SPEC-002 Rack Assembly Engine
// Alias semantico para numeros de serie. Hoy es un string simple; se separa
// en su propio tipo para que, si en un sprint futuro se necesita validacion
// o formato especifico por fabricante, el cambio quede localizado acá.

export type SerialNumber = string;
